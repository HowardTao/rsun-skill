# AGENTS.md — rsun-skill 仓库

技能集合仓库:存放可分发给 AI 客户端(WorkBuddy / CodeBuddy / pi 等)的数据技能包。

## 仓库约定

- 每个技能一个独立目录,**自包含**:脚本、参考资料、文档都在本目录内,不跨目录引用
- 技能目录至少包含 `SKILL.md` 与 `README.md`;涉及多轮任务的技能维护 `PROJECT_MEMORY.md`
- 新增技能后,在根 `README.md`「当前技能列表」登记一行(技能名 / 目录 / 一句话简介)
- 公共工具(如 guancli 认证、Python 解释器路径)由各技能在自己的文档内说明,仓库不设全局配置

## 铁律

1. **文档与代码必须同步**:改脚本功能 / 参数 / 输出列 / 数据源,必须同时更新该技能的 `SKILL.md`、脚本头部注释、该技能的 `README.md`,并在 `PROJECT_MEMORY.md` 变更日志追加一行(日期 + 变更摘要)。
2. **业务口径变更须先与负责人确认**,确认后记入 `PROJECT_MEMORY.md`「已确认的决策」,再动代码与文档。
3. **提交信息用中文**,简要描述变更内容。
4. `output/` 产物、`__pycache__/`、`*.pyc`、`.DS_Store` **不入库**。

## 权威文档分工

| 想知道什么 | 看哪里 |
|---|---|
| 仓库结构与分发约定 | 本文件 + 根 `README.md` |
| 某个技能的完整口径与执行流程 | **该技能目录的 `SKILL.md`**(权威) |
| 技能背景、已确认决策、变更历史 | 该技能目录的 `PROJECT_MEMORY.md` |
| 字段口径、别名映射等参考资料 | 该技能目录的 `references/` |

> 本文件只放**长期有效的仓库级约定**。具体业务口径写进对应技能的 `SKILL.md`,不要在这里重复 —— 重复的内容迟早会与权威文档不一致。
